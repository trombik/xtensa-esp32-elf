ESP32 series ROM ELF files

Files in the release archives have the following naming pattern:

<CHIP_NAME>_rev<REV>_rom.elf

<CHIP_NAME> - the name of the chip, lower case, without hyphens. For
example, esp32, esp32c3, esp32s2.

<REV> - chip revision. You can check the revision of a chip either using
esptool.py chip_id command or using ESP-IDF API esp_chip_info. If the
archive doesn't contain the ROM ELF file for your chip revision, use the
next lower one.

For example, for ESP32 rev. 0 and rev. 1, use esp32_rev0_rom.elf.
